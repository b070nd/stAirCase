/bin/
/var/
*.exe
*.test
*.prof
.DS_Store
.idea/
.tasks/
tasks/
.vscode/
.setup/
.claude/
.review/
graphify-out/
vendor/
/staircase
/staircase-*

# Python bytecode and cache (CHECK 2.2.3 — no compiled artifacts in VCS)
__pycache__/
*.pyc
*.pyo
*.pyd

# Built Go binaries under src/ (CHECK 2.2.3)
src/staircase

# Note: tests/fixtures/repos/*.tar.gz are intentional test fixtures (pre-built
# git repository snapshots required by the orchestrator integration tests).
# They are exempt from CHECK 2.2.3 because they are not build outputs —
# they cannot be regenerated without the original repo history.
